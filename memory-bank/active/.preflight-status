PASS WITH ADVISORY

Preflight verified the Level 2 prose/policy plan against the current entrypoint skill and its pytest contracts.

- [PASS] Prerequisites: planning is complete; no creative phase is required for this Level 2 task.
- [PASS] TDD plan encoding: the only unit changes a skill document, not executable behavior. The plan correctly avoids change-detector tests and schedules the existing suite as regression verification.
- [PASS] Convention compliance: the sole target, `skills/conformance-run/SKILL.md`, is the existing operator-only entrypoint; the planned opening placement and Step 2 reinforcement match its current structure and the repository's observation-over-judgment vocabulary.
- [PASS] Dependency impact and conflict detection: `tests/test_entrypoint_readme.py` already enforces frontmatter, driver flow, vocabulary, and prohibited judgment/coaching terms; `tests/test_leakage_lint.py` covers sentinel leakage. Existing instructions prohibit reading the checker before a step, and the proposed wording complements rather than duplicates that constraint by covering post-check remediation.
- [PASS] Completeness: all five brief requirements map to concrete edits and the existing `uv run pytest` verification command.

Advisory: consider a future harness-level guard that records a content digest for an artifact when its check runs and reports subsequent changes before the next step. This would make post-check remediation independently visible even when a driver ignores the instruction, but it is outside this focused prose change.
