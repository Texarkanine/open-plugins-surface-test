FAIL (fixable)

Findings:
- Fixable: Unit 2 introduces `check.py` default work-root delegation, but its numbered test steps cover only setup, hook recorder, and LSP callers. Add a test that loads `check.py` through its existing importlib helper with `CONFORMANCE_WORK` unset and verifies it resolves the shared cwd `plugintest/CURRENT` run (or otherwise explicitly cover this behavior in `tests/test_work_root.py`). This is required to encode the stated `check.py resolve_work_dir` behavior before production code.
- Pass: Units 1, 2, and 4 place test-writing and a red run before production code. Units 3 and 5 correctly exclude prose/policy from TDD.
- Pass: The proposed shared resolver, cwd-relative CURRENT symlink, and install-tree pointer preserve the reset boundary and avoid duplicating resolver behavior across shell, checker, and LSP callers.
- Pass: The planned files cover all brief requirements, including prompt/skill guidance, gitignore, setup reset behavior, LSP cwd divergence, and durable documentation.

Advisory:
- Consider a short integration test that runs `setup.sh`, `hook_record.sh`, and `check.py` from one temporary launch cwd without `CONFORMANCE_WORK`; it would prove the cross-process shared-run contract rather than only the individual resolver paths.
