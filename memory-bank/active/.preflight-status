PASS WITH ADVISORY

Findings from Preflight run on task harness-marketplace-manifests:

1. (Fixed in-phase, TDD Plan Encoding) Unit 2 ("README marketplace install") scheduled a test asserting README contains specific marketplace-add phrasing and the plugin name. That test can only go red when someone deliberately edits the README prose it asserts on — it does not verify any executable behavior (e.g. it cannot detect a broken marketplace add, only a missing sentence) — so it is a change-detector per `always-tdd.mdc`. README copy is user-facing prose/policy, which the TDD rule already exempts from owing tests. Struck the two test-scheduling steps and the corresponding "Behaviors to Verify" / "Existing test files to extend" lines in `tasks.md`; kept the production step (extend README) and the regression re-run of the existing suite. Unit 1 (the four JSON manifests) is unaffected — those are executable config read by real harness loaders, correctly TDD-staged (stub tests → stub interface → red → green), and not touched.

2. (Advisory) Web-verified against current Claude Code and Cursor plugin docs: `source: "./"` for Claude resolves relative to the marketplace root (not `.claude-plugin/`), so pointing at repo root via `"./"` is correct, and `.claude-plugin/plugin.json` sharing a directory with `marketplace.json` is exactly what `strict: true` (default) requires for a `source: "./"` entry — the plan's design already matches this. Omitting `version` from marketplace entries is also correct: Claude Code always prefers `plugin.json`'s `version` silently, so a stale marketplace-entry version would otherwise be masked. For Cursor, `additionalProperties: false` on marketplace plugin entries (confirmed via the `schemas/marketplace.schema.json` source referenced in cursor/plugins#281) really does limit entries to `name`/`source`/`description`/`minClientVersions` even though some Cursor docs pages list more fields — the plan's conservative allowlist is the correct choice, not overcautious.

3. (Advisory, unresolved by design) Two residual runtime uncertainties remain that pytest cannot gate, and the plan already says so explicitly (Challenges & Mitigations, Dependencies): (a) whether Cursor accepts a bare `"."` as a plugin `source` denoting the marketplace root itself — no doc example shows root-relative Cursor sourcing, only subdirectory names; (b) whether Claude's default folder-based component discovery (skills/, agents/, hooks/hooks.json, `.mcp.json`) actually fires for every component type when the thin `.claude-plugin/plugin.json` declares no explicit component paths (docs confirm this for `skills/` specifically; not explicitly confirmed for the others). Both are already correctly scoped as post-push operator verification, not a build gate — no plan change needed.

4. (Radical Innovation, advisory, not applied) The single most useful upgrade available here: instead of only hand-written pytest shape assertions, shell out from `tests/test_marketplace.py` to the actual upstream validators — Cursor's own `scripts/validate-plugins.mjs` (via `npx`, from the `cursor/plugins` repo) and/or `claude plugin validate .` when the `claude` CLI is present, skipping (not failing) when the tool is unavailable, mirroring the existing `uv`-absence skip convention in `techContext.md`. That would close most of finding 3's gap using the harnesses' own schema logic instead of a hand-rolled approximation, without requiring a live marketplace-add. Not applied to the plan — advisory only, per Preflight's "judge, do not fix" mandate.

Convention Compliance: PASS. File locations (`.claude-plugin/`, `.cursor-plugin/` at repo root) match the existing repo-root-manifest convention (`.plugin/`, `.mcp.json`, `.lsp.json`). Test file location/style (`tests/test_marketplace.py`, `ROOT = Path(__file__).resolve().parents[1]`) matches existing test files exactly.

Dependency Impact: PASS. `test_leakage_lint.py` / `lint_leakage.py` only scan `prompts/` and `skills/conformance-run/`; new manifest/README files are outside that scope. No pytest path restrictions in `pyproject.toml` — new test file auto-collected. `.plugin/plugin.json` and its existing contract test (`test_plugin_skeleton.py`) are untouched by the new files.

Conflict Detection: PASS. No existing `.claude-plugin/` or `.cursor-plugin/` directories in the repo. No duplication with `.plugin/plugin.json`. Public contract of `.plugin/plugin.json` and the probe tree is preserved — plan explicitly does not modify it.

Completeness Precheck: PASS. All four project-brief requirements and all four acceptance criteria map to concrete files/tests in Unit 1 (catalog + vendor manifest contracts) and Unit 2 (README instructions, now docs-only per finding 1).
