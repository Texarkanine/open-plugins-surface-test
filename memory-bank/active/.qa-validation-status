FAIL

Task: harness-marketplace-manifests (Level 2)
Scope reviewed: .claude-plugin/marketplace.json, .claude-plugin/plugin.json, .cursor-plugin/marketplace.json, .cursor-plugin/plugin.json, tests/test_marketplace.py, README.md Install section (commit e6e0bd8). Mechanical state: `uv run pytest` 174 passed, working tree clean.

BLOCKING

1. DRY / Integrity — unguarded triplicated plugin identity.
   `name`, `version`, and `description` for open-plugins-conformance now exist in three manifests (.plugin/plugin.json, .claude-plugin/plugin.json, .cursor-plugin/plugin.json) and the description a fourth and fifth time in the two marketplace entries. Only `name` is asserted in the vendor manifests; `version` and `description` are asserted nowhere. A version bump or description edit to the vendor-neutral manifest leaves the harness-native manifests silently stale, and each harness then reports a different version/description for the same plugin. This is the manifest/version-lockstep contract the project's TDD rule names explicitly as a legitimate (non-change-detector) test: it goes red when the contract breaks for a user, not when content is merely edited. The diff introduced the duplication and shipped no guard.
   Required change: add a lockstep contract test to tests/test_marketplace.py asserting both vendor plugin.json files agree with .plugin/plugin.json on name, version, and description, and that both marketplace entry descriptions match as well. Written test-first per TDD.

ADVISORY (non-blocking)

2. DRY — tests/test_marketplace.py::test_source_resolves_to_repo_root_plugin re-runs the COMPONENT_PATH_FIELDS "./"-prefix loop and the plugin-name assertion already owned by tests/test_plugin_skeleton.py::test_plugin_manifest_name_and_component_paths, and redeclares COMPONENT_PATH_FIELDS verbatim. The source-resolution test's own contract is the resolved-path == ROOT assertion; the manifest-shape re-check adds no coverage. Planned that way, so not held against the build.

3. Brittleness — the `names == VENDOR_MANIFEST_NAMES` exact-set assertions go red on any future legitimate file in .claude-plugin/ or .cursor-plugin/ (e.g. a README). The contract that matters is "no component directories nested here." Acceptable as written; worth loosening if it ever fires on a benign addition.

4. Residual runtime uncertainty carried forward from Preflight and correctly out of scope: Cursor bare "." source and Claude default component discovery under a thin plugin.json remain operator-verified after push, not pytest-gated. No QA objection.

CLEAN

- Completeness: all four manifests and the README Install rewrite are implemented; no stubs, TODOs, or pseudocode.
- YAGNI: marketplace entry `version` correctly omitted; Cursor entry keys held to name/source/description.
- Regression: .plugin/plugin.json and the repo-root probe tree untouched; systemPatterns.md invariants (observation over judgment, reset boundary, probe/prompt/artifact) unaffected.
- Integrity: no hardcoded shortcuts, magic values, or debug artifacts.
- Documentation: README leads with the marketplace add and demotes local path load to an unverified alternative, matching the brief; no probe fingerprints or judgment wording leaked in.

Next: /niko-build to add the lockstep test (red) and reconcile, then re-run /niko-qa.
