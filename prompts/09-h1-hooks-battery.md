# Step 9

In the work tree, perform this sequence:

1. Write a new file under `work/artifacts/`.
2. Edit that file (change its contents).
3. Read that file.
4. Run a shell command that succeeds.
5. Run a shell command that exits with a non-zero status.
