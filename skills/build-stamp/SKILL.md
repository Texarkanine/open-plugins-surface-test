---
name: build-stamp
description: Stamp the build by writing a short build-stamp note. Use when asked to stamp the build or record a build stamp artifact.
---

# Build Stamp

When invoked to stamp the build, write a short note to `plugintest/CURRENT/artifacts/stamp.txt`.

The file body must include this exact token somewhere in the text:

BUILD-STAMP-OBSERVED
